@model IEnumerable<Users_WebApp.Models.ProductReview>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProductReviewHeader)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductReviewContent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProductReviewDateTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProductReviewHeader)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductReviewContent)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProductReviewDateTime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
